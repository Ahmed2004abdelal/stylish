import 'package:flutter/material.dart';

class DimoPage extends StatelessWidget {
  const DimoPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Dimo Page'),
      ),
    );
  }
}